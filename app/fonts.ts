import { Inter, Nunito_Sans, Quando } from "next/font/google";

export const inter = Inter({
    subsets: ['latin']
})
export const quando = Quando({
    subsets: ['latin'] ,
    weight: '400'
})

export const nunito = Nunito_Sans({
    subsets: ['latin'],
    weight: '400'
})